{% comment %} noll Blog (Journal) — editorial article list — bilingual {% endcomment %}

<nav class="crumbs" aria-label="breadcrumb">
  <a href="{{ routes.root_url }}" data-en="Home">Home</a><span>/</span>
  <span data-en="Journal">Journal</span>
</nav>

<header class="blog-hero">
  <p class="blog-hero__eyebrow reveal" data-en="{{ section.settings.eyebrow_en | default: 'noll Journal' | escape }}">{{ section.settings.eyebrow | default: 'noll ジャーナル' }}</p>
  <h1 class="blog-hero__title reveal" data-reveal-delay="1" data-en="{{ section.settings.hero_title_en | default: 'Notes on Walking & Thinking' | escape }}">{{ section.settings.hero_title | default: '歩行と思考、その記録。' }}</h1>
  <p class="blog-hero__sub reveal" data-reveal-delay="2" data-en="{{ section.settings.hero_sub_en | default: 'Research, essays, and field stories on walking, design, and the body.' | escape }}">{{ section.settings.hero_sub | default: '歩くこと、デザイン、身体について。リサーチ・エッセイ・フィールドストーリー。' }}</p>
</header>

{%- assign main = blog.articles.first -%}
{%- if main -%}
  <article>
    <div class="journal-hero">
      <p class="journal-hero__cat reveal">{% if main.tags.first != blank %}{{ main.tags.first | upcase }}{% else %}FEATURED{% endif %}</p>
      <h2 class="journal-hero__title reveal" data-reveal-delay="1">{{ main.title }}</h2>
      <div class="journal-hero__meta reveal" data-reveal-delay="2">
        {{ main.published_at | date: '%Y.%m.%d' }} · {{ main.author }}
      </div>
    </div>
    <div class="journal-hero__media reveal">
      <div class="journal-hero__media-inner">
        {%- if main.image != blank -%}
          <img src="{{ main.image | image_url: width: 1600 }}" alt="{{ main.title | escape }}">
        {%- else -%}
          <img src="{{ 'journal_1.jpg' | asset_url }}" alt="{{ main.title | escape }}">
        {%- endif -%}
      </div>
    </div>
    <div style="text-align:center; margin: 32px 0 64px;">
      <a href="{{ main.url }}" class="btn btn--solid-dark" data-en="{{ section.settings.read_cta_en | default: 'Read article →' | escape }}">{{ section.settings.read_cta | default: '記事を読む →' }}</a>
    </div>
  </article>
{%- endif -%}

<section class="journal-related">
  <h2 data-en="{{ section.settings.list_title_en | default: 'All Articles' | escape }}">{{ section.settings.list_title | default: 'すべての記事' }}</h2>
  <div class="journal-related__grid">
    {%- assign fallback_imgs = 'journal_2.jpg,journal_3.jpg,journal_1.jpg,insta_1.jpg,insta_2.jpg,insta_3.jpg' | split: ',' -%}
    {%- for art in blog.articles offset: 1 limit: 12 -%}
      {%- assign fb = fallback_imgs[forloop.index0] | default: 'journal_1.jpg' -%}
      <a href="{{ art.url }}" class="journal-card reveal"{% if forloop.index0 > 0 %} data-reveal-delay="{{ forloop.index0 }}"{% endif %} data-cursor="view">
        <div class="journal-card__media">
          {%- if art.image -%}
            <img src="{{ art.image | image_url: width: 800 }}" alt="{{ art.title | escape }}">
          {%- else -%}
            <img src="{{ fb | asset_url }}" alt="{{ art.title | escape }}">
          {%- endif -%}
        </div>
        <p class="journal-card__cat">{{ art.tags.first | default: 'Journal' }}</p>
        <h3 class="journal-card__title">{{ art.title }}</h3>
        {%- if art.excerpt != blank -%}
          <p class="journal-card__excerpt">{{ art.excerpt | strip_html | truncate: 90 }}</p>
        {%- endif -%}
        <p class="journal-card__date">{{ art.published_at | date: '%Y.%m.%d' }}</p>
      </a>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "noll Blog",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow (JA)", "default": "noll ジャーナル" },
    { "type": "text", "id": "eyebrow_en", "label": "Eyebrow (EN)", "default": "noll Journal" },
    { "type": "text", "id": "hero_title", "label": "Hero title (JA)", "default": "歩行と思考、その記録。" },
    { "type": "text", "id": "hero_title_en", "label": "Hero title (EN)", "default": "Notes on Walking & Thinking" },
    { "type": "textarea", "id": "hero_sub", "label": "Hero sub (JA)", "default": "歩くこと、デザイン、身体について。リサーチ・エッセイ・フィールドストーリー。" },
    { "type": "textarea", "id": "hero_sub_en", "label": "Hero sub (EN)", "default": "Research, essays, and field stories on walking, design, and the body." },
    { "type": "text", "id": "list_title", "label": "List title (JA)", "default": "すべての記事" },
    { "type": "text", "id": "list_title_en", "label": "List title (EN)", "default": "All Articles" },
    { "type": "text", "id": "read_cta", "label": "Read CTA (JA)", "default": "記事を読む →" },
    { "type": "text", "id": "read_cta_en", "label": "Read CTA (EN)", "default": "Read article →" }
  ],
  "presets": [{ "name": "noll Blog" }]
}
{% endschema %}
